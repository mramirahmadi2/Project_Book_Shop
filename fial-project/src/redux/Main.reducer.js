const inaitailState ={
    product:[
       { id:1,
        title: "new one"
    }
    ],
    selectedProduct:null
};
const MainReducer = (state = inaitailState, action)=>{
    return state;
}

export {MainReducer};
